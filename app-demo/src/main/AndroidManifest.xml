<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.defince.appdemo">

    <application
        android:name=".DemoApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher_demo"
        android:label="${app_name}"
        android:roundIcon="@mipmap/ic_launcher_demo_round"
        android:supportsRtl="true"
        android:theme="@style/Watch2Earn.AppTheme">

        <activity
            android:name=".DemoActivity"
            android:theme="@style/Watch2Earn.AppThemeSplash"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="com.defince.appdemo"
                    android:scheme="app" />
            </intent-filter>
        </activity>
    </application>
</manifest>
